type Query {
  product(id: ID!): Product
  user(id: ID!): User
}

type Product {
  id: ID!
  name: String!
  price: Float!
  description: String
}

type User {
  id: ID!
  name: String!
  email: String!
}
